import { Text, View, StyleSheet } from "react-native";
import {  } from "react-native-paper";

const Name = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
}

const styles = StyleSheet.create({});

export default Name;


--------------

Lesson 10:

npx expo install react-native-paper
(if necessary) npx expo install react-native-safe-area-context


--------------

Lesson 9:

npx expo install validator


--------------

Lesson 6:

npx expo install @react-navigation/drawer
npx expo install react-native-gesture-handler
npx expo install react-native-reanimated
or
npx expo install @react-navigation/drawer react-native-gesture-handler react-native-reanimated


start the development server and clear the babel cache: npx expo start --clear


--------------

Lesson 5:

npx expo install expo-router
npx expo install react-native-safe-area-context
npx expo install react-native-screens
npx expo install expo-linking
npx expo install expo-constants
npx expo install expo-status-bar
npx expo install react-native-gesture-handler
or
npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar react-native-gesture-handler


--------------

Lesson 3:

Xcode must be fully installed before you can continue [Y/n]
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer


npx expo install --fix

npx expo install react-native-safe-area-context


--------------

Lesson 2:

https://icons.expo.fyi